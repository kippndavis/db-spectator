Barebones automated game spectator using Python + Playwright for Duelingbook.

Once Python is installed, run:
- pip install -U pip
- pip install playwright
- python -m playwright install chromium
- python DB_Spectate.py
